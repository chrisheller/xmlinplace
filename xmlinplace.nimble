# Package

version     = "0.1.0"
author      = "Chris Heller"
description = "Update XML in place without extraneous changes"
license     = "MIT"

# Deps

# This may work on earlier Nim versions as well, but has not been tested
requires "nim >= 1.4.0"

# These are just for the test code and are not generally required
requires "diff >= 0.5.0"
requires "diffoutput >= 0.1.2"
